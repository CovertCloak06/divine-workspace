{
  "name": "@divine/workspace",
  "version": "1.0.0",
  "private": true,
  "description": "Divine Node monorepo workspace - THE master template",
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "test": "turbo run test",
    "lint": "turbo run lint",
    "format": "turbo run format",
    "format:check": "turbo run format:check",
    "clean": "turbo run clean && rm -rf node_modules"
  },
  "keywords": [
    "monorepo",
    "workspace",
    "template"
  ],
  "author": "Divine Node",
  "license": "MIT",
  "packageManager": "pnpm@10.28.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "devDependencies": {
    "turbo": "^2.7.3"
  },
  "dependencies": {
    "@biomejs/biome": "^2.3.11"
  }
}
